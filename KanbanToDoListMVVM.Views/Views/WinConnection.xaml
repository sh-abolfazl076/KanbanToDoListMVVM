<UserControl x:Class="KanbanToDoListMVVM.Views.Views.WinConnection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KanbanToDoListMVVM.Views.Views"
             mc:Ignorable="d" 
             d:DesignHeight="445" d:DesignWidth="300" Width="300" Height="445">
    <Grid Margin="20,20,20,44">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="{Binding Title}" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Inputs -->
        <StackPanel Grid.Row="1" Margin="10" VerticalAlignment="Top" >
            <Label Content="Server"/>
            <TextBox Height="25" Margin="0,0,0,10"
                     Text ="{Binding ServerName , UpdateSourceTrigger=PropertyChanged }"
                     />

            <Label Content="Database"/>
            <TextBox Height="25" Margin="0,0,0,10"
                     Text ="{Binding DatabaseName , UpdateSourceTrigger=PropertyChanged }"
                     />

            <Label Content="User"/>
            <TextBox Height="25" Margin="0,0,0,10"
                     Text ="{Binding UsernameDatabase , UpdateSourceTrigger=PropertyChanged }"
                     />

            <Label Content="Password"/>
            <TextBox Text ="{Binding PasswordDatabase , UpdateSourceTrigger=PropertyChanged }" Height="25"/>
            <Label FontWeight="Bold" FontSize="16" FontFamily="B Titr" Margin="0,0,0,0" Foreground="#DC3545" Content="{Binding CheckConnectionLabal}"/>
        </StackPanel>

        <!-- Button -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,0,0,0">
            <Button Content="Check Connection" 
                    Width="140" Height="39"  Margin="0,0,12,0"
                    Background="#2196F3" Foreground="White" 
                    FontWeight="Bold" 
                    Command="{Binding CheckConnectionCommand}"            
                    />

        </StackPanel>
    </Grid>
</UserControl>
